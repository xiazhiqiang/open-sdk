{
  "name": "opensdk",
  "version": "1.0.0",
  "private": true,
  "description": "前端开放 SDK 设计及实现 Demo",
  "keywords": [],
  "homepage": "https://github.com/xiazhiqiang/opensdk#readme",
  "bugs": {
    "url": "https://github.com/xiazhiqiang/opensdk/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xiazhiqiang/opensdk.git"
  },
  "license": "ISC",
  "author": "",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "yarn install --ignore-engines",
    "build": "lerna run build --stream --sort",
    "clean": "./cleanup.sh",
    "prepare": "husky install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "dependencies": {},
  "devDependencies": {
    "@commitlint/cli": "^17.6.5",
    "@commitlint/config-conventional": "^17.6.5",
    "husky": "^8.0.3",
    "lerna": "^6.0.0"
  }
}
